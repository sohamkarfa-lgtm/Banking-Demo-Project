-- Total transaction amount per customer
SELECT customer_id, SUM(amount) total_amount
FROM banking_catalog.gold.transaction_fact
GROUP BY customer_id;

-- Account balances
SELECT customer_id, SUM(balance)
FROM banking_catalog.gold.account_dim
GROUP BY customer_id;
